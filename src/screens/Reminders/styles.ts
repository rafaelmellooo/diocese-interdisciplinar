import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1
    },

    card: {
        marginVertical: 10,
        padding: 20,
        borderWidth: 1,
        borderRadius: 10,
    },

    cardTitle: {
        fontWeight: 'bold',
        fontSize: 16
    }
});