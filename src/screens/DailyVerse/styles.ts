import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        margin: 30
    },
    title: {
        fontSize: 32,
        fontWeight: 'bold',
        textAlign: 'center'
    },
    text: {
        fontSize: 24,
        textAlign: 'justify'
    }
});